# ── Secrets & connexions — jamais commit tel quel ────────────────────────
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/blogueur_ia
SERPER_API_KEY=
LLM_API_KEY=

# Chemin vers le fichier de config comportementale (voir blogueur_ia.conf.example)
BLOGUEUR_IA_CONF=blogueur_ia.conf